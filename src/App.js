import "./App.css";
import CaptionCarousel from "./CaptionCarousel";
import Counter from "./Counter";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <CaptionCarousel />
        <Counter />
      </header>
    </div>
  );
}

export default App;
